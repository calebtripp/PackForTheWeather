@using PackForTheWeather.ViewModel
@model GetTripInfoViewModel

@foreach (var item in @Model.PackList)
{
    @if (item.Value == 0)
    {
        continue;
    }
    <span>□  @item.Key:     </span> <span>@item.Value</span>
    <br />
}