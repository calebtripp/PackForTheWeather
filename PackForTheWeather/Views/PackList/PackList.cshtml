@model PackListDictionary

<h2>Pack List</h2>
<table class="table">
    <tr>
        <th>qTY</th>
        <th>Name</th>        
    </tr>

    @foreach (var key in @Model.packList)
    {
        <tr>
            <td></td>
           
        </tr>
    }

</table>

// iterate through keys, only display values above 0

	foreach (var kvp in suitcase) 
    {
        if (kvp.Value > 0)      
        {
        Console.WriteLine("□  {0} {1}", kvp.Key, kvp.Value);
        }
    }